@using CommunicationsApp.Domain.Entities;
@using Microsoft.AspNetCore.Identity
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <p class="nav-link text-dark">Hello @User.Identity?.Name!</p>
        </li>
        <li class="nav-item">
            <form asp-controller="Accounts" asp-action="LogOff" >
                <button type="submit" class="nav-link btn btn-link text-dark">Log out</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Accounts" asp-action="SignUp">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Accounts" asp-action="Login">Login</a>
        </li>
    }
</ul>
